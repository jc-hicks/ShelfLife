# MongoDB connection string (local instance or Atlas cluster).
# The app connects to the "shelflife" database on this URI.
MONGODB_URI=mongodb://localhost:27017

# Optional — port the Express server listens on (defaults to 3000).
PORT=3000
